#include <stdio.h>
#include "structure.c"

void recuperation_facture(int factureID){
    for (int i = 0; i < 3; ++i) {
        if (liste_Facture[i].ID == factureID){
            printf()
        }
    }
}